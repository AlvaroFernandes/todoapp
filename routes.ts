//Array of public routes @type {string[]}
export const publicRoutes = ["/"];

//Array of routes used for authentication @type {string[]}
export const authRoutes = ["/auth/login", "/auth/register"];

//Prefix for api authentication @type {string}
export const apiAuthPrefix = "/api/auth";

//Default redirect @type {string}
export const DEFAULT_LOGIN_REDIRECT = "/settings";
